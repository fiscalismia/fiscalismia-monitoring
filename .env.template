# =============================================================================
# Fiscalismia Monitoring Environment Variables
# =============================================================================
# SECURITY: This file contains secrets. Do not commit to version control!
# Copy this template to .env and fill in the actual values on the server.
# =============================================================================

# Grafana Admin Credentials
# Generate a strong password: openssl rand -base64 32
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=CHANGE_ME_GENERATE_STRONG_PASSWORD

# Grafana Secret Key (used for signing cookies and other internal operations)
# Generate with: openssl rand -base64 32
GRAFANA_SECRET_KEY=CHANGE_ME_GENERATE_SECRET_KEY

# =============================================================================
# Optional: Additional Configuration
# =============================================================================

# Prometheus retention (can also be set in Dockerfile CMD)
# PROMETHEUS_RETENTION_TIME=30d
# PROMETHEUS_RETENTION_SIZE=10GB
